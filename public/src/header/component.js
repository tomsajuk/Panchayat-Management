(function() {
    'use strict';

    function controller($scope) {

    }

    angular.module('header').component('header', {
        templateUrl: 'src/header/template.html',
        controller: controller
    })
})();
